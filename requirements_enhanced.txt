# Oreja Enhanced Transcription Features
#
# This file used to duplicate (and drift from) backend/requirements.txt with
# its own separate version pins. It now simply extends that file so there is
# exactly one place package versions are pinned. Install from the repo root:
#
#   pip install -r requirements_enhanced.txt
#
# Everything the enhanced summarization / sentiment / annotation pipeline
# needs (torch, torchaudio, librosa, vaderSentiment, textblob, transformers,
# nltk, scipy, ...) already lives in backend/requirements.txt. Nothing in
# this file should ever pin a version of a package that is also pinned
# there - if you need a newer/older version, change it in
# backend/requirements.txt so both entry points stay in sync.
#
# The optional local-LLM summarizer (backend/llm_summarizer.py, talks to a
# locally-running Ollama server) adds NO new Python package - it only uses
# the standard library's urllib.

-r backend/requirements.txt
